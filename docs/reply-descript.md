Hi Harold,

Good news — everything you and Chris described is doable. Cataloguing the guests, topics and interviewers, a searchable database behind it, the internal "have we already run this" lookup, and a search box on the site for listeners. All of it.

And yes, we can definitely use the Descript transcripts. That actually helps a lot: since you've been correcting them while editing, the guest names and union names are already right — that's the part automated transcription usually gets wrong. Where projects have been deleted we can fill in the gaps, so partial coverage is no problem.

Whenever Chris and Patrick get a look at what's there, two things would help me: which export format is easiest (Word, plain text, or SRT), and whether speakers have been named in the projects.

No rush on any of it. Happy to talk it through whenever.

Paul
